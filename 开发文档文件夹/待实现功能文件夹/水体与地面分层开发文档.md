# 水体与地面分层开发文档

状态：待实现

## 1. 目标

把“地面”和“液体”彻底分开：

```text
地块系统
→ 负责地面、湖底、河床、海底

Liquid 系统
→ 负责这里是什么液体、液体有多深
```

核心规则：

> 抽水只能改液体深度，不能在抽水时生成或修改湖底 / 河床 / 海底。

---

## 2. Liquid 层保存什么

Liquid 层核心只有：

```text
LiquidDepth     = 当前液体深度，0~1
LiquidTypeIndex = 当前运行时使用的液体数字编号
```

另外有一个长期使用的液体名字：

```text
LiquidId
```

### LiquidId

LiquidId 是液体的固定名字，给 JSON、MOD、存档使用。

例如：

```text
core:dirty_water
core:sea_water
modId:acid
```

只要液体还是同一种东西，这个名字就不要随便改。

### LiquidTypeIndex

运行时没必要让每个格子都保存一串字符串，所以游戏加载全部液体后，临时给它们编号：

```text
0 = 没液体
1 = core:dirty_water
2 = core:sea_water
3 = modId:acid
```

Chunk 格子运行时保存这个数字，可以省内存、查得快。

但这个数字可能因为 MOD 或加载内容变化而改变，所以：

- JSON 不保存它。
- MOD 不保存它。
- 存档不保存它。
- 存档保存 LiquidId。
- 进入游戏后再把 LiquidId 转成 LiquidTypeIndex。

关系：

```text
LiquidId
→ 游戏加载液体配置
→ 转成 LiquidTypeIndex
→ Chunk 运行时使用
→ 保存时再保存 LiquidId
```

### LiquidDepth

LiquidDepth 表示这一格现在有多深：

```text
0.0 = 没液体
0.1 = 1/10 深度
0.5 = 一半深度
1.0 = 满深度
```

它就是现在项目里 `WaterDepth` 的改名，不是新加第二套深度系统。

这次实现时，把现有字段、变量、API、测试里的：

```text
WaterDepth
```

统一改成：

```text
LiquidDepth
```

---

## 3. 地面怎么处理

`GroundTileId` 继续完全属于地块系统，不放进 Liquid 层。

世界生成时就要把水下面真正的地块生成出来：

```text
海洋
→ 先生成海底 Ground
→ 再生成 core:sea_water + LiquidDepth

湖泊 / 河流
→ 先生成湖底 / 河床 Ground
→ 再生成 core:dirty_water + LiquidDepth
```

以后抽水：

```text
只减少 LiquidDepth
→ LiquidDepth 变成 0
→ 液体消失
→ 原本已经存在的底部地块露出来
```

抽水前后 GroundTileId 必须完全不变。

---

## 4. height 怎么处理

`height` 只在世界生成时临时计算。

它可以通过：

```text
世界坐标 + Seed + 冻结的世界生成规则
```

重新算出来，所以 Liquid 系统没必要长期保存它，也不用写进存档。

生成流程：

```text
坐标 + Seed
→ 算 height
→ 算地形 / 群系 / 初始 LiquidDepth
→ Chunk 生成完成
→ 后面直接使用 LiquidDepth
```

现在还有一些旧代码会：

```text
height
→ 再反算水深
```

这次要改成直接读取 LiquidDepth。

重点检查 Renderer、Navigation、TileEffect、Farmland 等旧引用。

---

## 5. 河流数据

下面这些不是 Liquid 层核心数据，继续留在原来的水文 / 世界生成系统：

```text
riverKind   = 这里是不是河流 / 湖泊之类
riverFlow   = 水流强度
riverFlowX  = 左右方向
riverFlowY  = 上下方向
```

Liquid 层不要再复制它们。

---

## 6. Chunk 最终关系

```text
ChunkTerrainData

地块数据
└─ TerrainCell[]
   └─ GroundTileId

Liquid 数据
├─ LiquidDepth[]
└─ LiquidTypeIndex[]
```

其它温度、河流、污染等数据继续属于自己的系统。

`TerrainCell.Flags.Water` 如果迁移期间必须保留，只能暂时兼容旧代码。

最终判断这一格有没有液体：

```text
LiquidDepth > 0
```

---

## 7. Liquid JSON

液体配置继续放在：

`Assets/StreamingAssets/GameConfig/Liquids/liquids.json`

LiquidId 继续作为液体身份。

世界里的液体可以在定义中加入显示和玩法配置，例如：

```json
"worldWater": {
  "spriteAddress": "...",
  "materialAddress": "...",
  "navigationCost": 20000,
  "shallowMoveSpeedMultiplier": 0.5,
  "deepMoveSpeedMultiplier": 0.2,
  "entryTemperatureFloor": 10.0
}
```

JSON 只保存配置和资源地址；Sprite、Material、Shader 仍是 Unity 资源。

当前 `Tile_Water_Fresh / Tile_Water_Salt` 还是旧“水也是地块”的结构。

先完成 Ground / Liquid 分层，再迁移旧 Water Tile，不要继续给旧结构加新功能。

---

## 8. 深水上浮时，脚下地块不能影响玩家

增加状态：

```text
LiquidFloating
```

它表示角色现在正在消耗游泳体力维持上浮，没有踩到底部地块。

触发条件复用现有游泳规则：

```text
LiquidDepth > SwimmingDepthThreshold
并且还有游泳体力
并且当前正在维持上浮
→ LiquidFloating = true
```

当 `LiquidFloating = true`：

- 脚下 Ground Behaviour 不生效。
- Liquid Behaviour 继续生效。
- 潮湿、体温、游泳、体力、氧气、液体减速、饮用等都不能被关掉。

例如：

```text
脚下是减速泥地
玩家正在水面上浮
→ 泥地减速不生效
```

下面情况要恢复 Ground Behaviour：

- 水变浅。
- 体力耗尽，开始下沉并重新接触底部。
- 离开液体。
- 其它原因结束上浮。

状态切换要求：

```text
开始上浮
→ Ground Behaviour 只 OnExit 一次

重新接触地面
→ Ground Behaviour 只 OnEnter 一次
```

不能每帧重复 OnExit / OnEnter。

现有 `effectSuppressors` 会把整个 TileEffectReceiver 都关掉，所以不能直接用它。

应该分成：

```text
Ground Behaviour
→ LiquidFloating 时暂停

Liquid Behaviour
→ 只要有液体就继续运行
```

---

## 9. 存档怎么保存

不要保存整个 Chunk 每一格的液体。

世界原本的液体可以靠 Seed 重新生成，所以存档只记录“玩家改过的液体格”。

例如原本：

```text
A格 = core:sea_water，深度 0.8
B格 = core:sea_water，深度 0.6
```

玩家只把 A 格抽到 0.3。

存档只记录：

```text
A格位置
LiquidId = core:sea_water
LiquidDepth = 0.3
```

B 格没改，不保存。

加载时：

```text
1. 先根据 Seed 生成原世界
2. 再读取存档里玩家改过的液体格
3. 用保存的数据覆盖这些格子
4. 最后再让显示、导航等系统开始使用这些数据
```

每个被修改的液体格保存：

```text
格子位置 + LiquidId + LiquidDepth
```

不要保存 LiquidTypeIndex。

---

## 10. 实施顺序

### P0：世界生成

- [ ] 海洋、河流、湖泊先生成真正的湖底 / 河床 / 海底 Ground。
- [ ] GroundTileId 不再表示 Water。
- [ ] 抽水不能修改 GroundTileId。

### P1：独立 Liquid 数据

- [ ] ChunkTerrainData 增加 LiquidDepth。
- [ ] ChunkTerrainData 增加 LiquidTypeIndex。
- [ ] 海洋、河流、湖泊、地下水直接生成初始 LiquidDepth。
- [ ] 全项目 WaterDepth 改名为 LiquidDepth。
- [ ] 删除运行时通过 height 反算水深的旧逻辑。

### P2：显示

BRG 是项目现在用来批量画地块和水面的渲染系统。

- [ ] Ground BRG 只画 GroundTileId。
- [ ] Liquid BRG 根据 LiquidTypeIndex 找到对应 LiquidDefinition。
- [ ] Liquid BRG 使用 LiquidDepth 显示液体深度。
- [ ] 岸边判断改为 LiquidDepth > 0。
- [ ] 保留现有四角水深平滑效果。

### P3：玩法

- [ ] Buff、体温、游泳、氧气、体力都读取 LiquidDepth。
- [ ] Navigation / FlowField / AIECS 都读取 LiquidDepth。
- [ ] 喝水通过 LiquidId 找对应的 LiquidDefinition。
- [ ] 抽水通过统一的 Liquid 修改接口改变 LiquidDepth。
- [ ] 旧 Tile_Water 的液体逻辑迁出去。
- [ ] 增加 LiquidFloating。
- [ ] LiquidFloating 只暂停 Ground Behaviour，不暂停 Liquid Behaviour。

### P4：存档

- [ ] 只保存玩家改过的液体格。
- [ ] 每格保存：位置 + LiquidId + LiquidDepth。
- [ ] 没改过的格子继续靠 Seed 重新生成。
- [ ] 加载时先生成原世界，再覆盖玩家修改。
- [ ] 覆盖完成后再启动显示、导航和其它使用 Liquid 数据的系统。

### P5：清理旧 Water Tile

先迁移：

- [ ] 海洋 Biome。
- [ ] MapCore 河流配置。
- [ ] ChunkTilePalette_Default。
- [ ] 地面 / Legacy Map Prefab。

确认已经没有引用后再删除：

- [ ] Tile_Water_Fresh。
- [ ] Tile_Water_Salt。
- [ ] 没用途的 Water TileBase。

### P6：抽水机

- [ ] 抽水只减少 LiquidDepth。
- [ ] LiquidDepth = 0 后液体不再显示。
- [ ] 原本生成好的底部 Ground 直接露出来。
- [ ] GroundTileId 保持不变。

---

## 11. 完成后检查

### 地面 / 液体

- [ ] 水下面一直有真正的 Ground。
- [ ] 抽水前后 GroundTileId 完全一样。
- [ ] LiquidDepth = 0 后能正确露底。
- [ ] 淡水 / 海水还能正确区分。

### LiquidDepth

- [ ] Renderer 使用 LiquidDepth。
- [ ] Buff / 体温 / 游泳 / 氧气 / 体力使用 LiquidDepth。
- [ ] Navigation / FlowField / AIECS 使用 LiquidDepth。
- [ ] 运行时不再通过 height 算当前液体深度。

### 上浮

- [ ] 正在消耗体力上浮时，Ground Behaviour 不生效。
- [ ] Ground 暂停期间，Liquid Behaviour 仍正常。
- [ ] 体力耗尽、水变浅、离水后 Ground Behaviour 都能正确恢复。
- [ ] 不会每帧重复调用 OnEnter / OnExit。

### 存档

- [ ] 抽水后保存再进入，水深保持玩家修改后的结果。
- [ ] 没改过的液体格不写进存档。
- [ ] Chunk 卸载再加载后结果正确。
- [ ] LiquidId 能正确恢复液体种类。
- [ ] 存档不保存 LiquidTypeIndex。

### 最后

- [ ] 没有新增 Error。
- [ ] 没有新增 Warning。
- [ ] 删除旧 Water Tile 前确认所有引用都已经迁走。

---

## 12. 这次不做

这次先不要扩大任务，不做：

- 液体自动向旁边流动。
- 无限水源 / 自动补水。
- 水压。
- 洪水传播。
- 蒸发和降雨的水量守恒。
- 水坝。
- 不同液体自动混合。

这次只把三件事做好：

```text
1. 地面和液体真正分开
2. 所有系统统一使用 LiquidDepth
3. 抽水后露出世界生成时已经存在的底部地块
```

