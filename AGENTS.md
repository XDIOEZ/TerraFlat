# FlatWorld 项目编程指南

- 本项目是 Unity 2D 俯视角沙盒游戏。
- 功能验收统一使用真实 Play Mode / GamePlayMCP 运行链；不再新增或恢复 `Assets/GameTest`、Unity Test Runner、冒烟测试或独立静态验证脚本。编译与 Console 只作为进入实机运行前的门禁和故障证据，不作为独立验收结果。
- 处理游戏系统任务时，根据任务类型直接读取对应的 `.agents/skills/flatworld-*/SKILL.md` 专项 Skill；任务跨系统时只读取直接相关的多个 Skill，不要无目的地搜索整个项目。
- 完成代码或资源修改后，必须检查本次使用的专项 Skill 是否需要同步；只记录系统维护中可复用的易错点、隐含约束和必要注意事项,以及系统架构
- 清理无效、未使用的代码。
- 使用 `#region` 以脚本内的系统为单位包裹代码块。
- 编写代码时添加中文注释：类级注释应详细说明用途和关键数值；方法和字段使用一句话或关键词概括即可。
- 以扩展优先设计系统：功能和数据按可注册、可替换、可独立持久化的模块/观察者拆分，避免把玩法细节和配置不断堆进单个中心脚本；新增机制应通过通用契约、状态容器或注册表接入。
- 始终要考虑后续游戏的MOD的接入
- 编码时兼顾 Harmony 等代码 MOD 的 Patch 友好性：关键玩法节点优先拆成职责明确、语义稳定、可独立定位的具名方法，避免把重要逻辑长期埋在超大方法、匿名委托、Lambda、本地函数、属性访问器或难以定位的编译器生成代码中。
- 面向 MOD 的关键方法应尽量保持类名、方法名、参数含义与返回语义稳定；确需重构公开或常用 Patch 边界时，优先保留兼容转发入口或提供明确迁移路径，避免无必要的签名漂移。
- 关键玩法流程优先形成“进入前 / 核心执行 / 结果产出”等清晰方法边界，使 MOD 作者可以用 Prefix / Postfix 完成常见扩展，而不必依赖脆弱的 IL Transpiler；不要为了 Patch 方便破坏正常封装，可通过稳定方法、事件、接口或注册点提供扩展边界。
- 对明确作为 Harmony 稳定 Patch 点的方法，避免无意义泛型化、过度内联式写法和频繁搬迁；仅在确有 Harmony Patch 稳定性需求时考虑显式禁止内联，不要全项目滥用 `NoInlining`。
- 读到了这个文件 在对话后面加上一个符合当前对话语境的颜文字
