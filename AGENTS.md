# 项目协作要求

- 回答时尽量只回答重点，简洁干练。
- 修改或诊断游戏系统前，先使用 `$flatworld-system-map` 定位系统，再读取对应 FlatWorld 子 Skill 的文件地图；仅当地图缺失或失效时才进行全项目搜索，并将新发现的权威入口补回对应 Skill。
- 每次修改代码后，必须在交付前编译项目，并报告编译结果；若编译被已有错误阻断，需明确说明阻断原因。
- 不得通过桌面自动化接管显示器，也不得模拟键鼠操作 Unity Editor 或其他桌面应用。
- 每次代码修改完成后，通过 Unity MCP Skill（端口 `6400`）刷新 Unity 资源（等效 `Ctrl+R`），不得为此接管显示器。
- 除上述资源刷新外，功能验证、Unity 内操作与测试均由用户手动完成；实现时提供清晰、方便的测试入口即可。
- 每次任务完成、准备发送最终答复前，先通过 Codex `list_threads` 检查其他对话；仅当没有其他 `active` 或 `queued` 对话时，运行 `Tools\CodexTaskCompleteAlert.ps1` 提示用户。若仍有其他待完成对话则不提示；除非用户明确要求静默。
