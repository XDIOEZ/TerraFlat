# FlatWorld 项目编程指南

- 灵活使用 MCP 和 Skill，减少不必要的 Token 消耗。
- 完成工作后提供简短的总结反馈。
- 本项目是 Unity 2D 俯视角沙盒游戏。
- 处理游戏系统任务时，根据任务类型直接读取对应的 `.agents/skills/flatworld-*/SKILL.md` 专项 Skill；任务跨系统时只读取直接相关的多个 Skill，不要无目的地搜索整个项目。
- 完成代码或资源修改后，必须检查本次使用的专项 Skill 是否需要同步；只记录系统维护中可复用的易错点、隐含约束和必要注意事项,以及系统架构
- 只有新发现的信息能帮助后续工作避坑时才更新专项 Skill；已经由代码清楚表达、仅描述“这次改了什么”的内容不要写入 Skill。

## 编程规范

- 积极清理无效、未使用的代码。
- 积极使用 `#region` 包裹相关代码块。
- 编写代码时添加中文注释：类级注释应详细说明用途和关键数值；方法和字段使用一句话或关键词概括即可。
- 以扩展优先设计系统：功能和数据按可注册、可替换、可独立持久化的模块/观察者拆分，避免把玩法细节和配置不断堆进单个中心脚本；新增机制应通过通用契约、状态容器或注册表接入。
- 需要人工测试时，在任务完成后明确告诉用户应测试什么。

## 不确定问题处理

- 先读取相关代码、配置和专项 Skill；关键事实、预期行为、目标平台、资源归属或改动边界仍不明确时，暂停实现并向用户提出最少且具体的问题。
- 不用猜测补齐需求；代码证据能够确认的内容直接确认，无法确认的内容列出可选方案及影响后询问。
- 仅在低风险且可逆的默认选择下继续，并先说明假设；涉及删除或覆盖资源、公共接口、存档、网络协议或跨平台行为时必须先确认。
- 当前项目不要求兼容旧存档；调整存档或序列化结构时按当前版本直接实现，不新增旧存档迁移、兼容适配或历史字段回退逻辑。旧存档无法读取时应明确报版本不兼容并停止加载，不得静默修补或覆盖。
- 不因用户当前打开某个文件就默认修改它，先定位真实实现入口与依赖关系。

## 其他

- 如果读取了本文档，在最终回复末尾添加符合当前情境的颜文字，并注意保持多样性。
- 只在确有必要时使用子智能体。
- 一旦已经定位到明确实现位置，不继续进行泛化搜索。
  只有出现依赖不明确时才扩大搜索范围。
- 阶段性压缩上下文,不是只有在满的时候才压缩,检测到编辑的系统出现跨度较大的更改的时候压缩上下文

回答时尽量只回答重点 简洁干练 通俗的介绍修改的逻辑和方式
精简到只剩三句话就最好了 就两三句话总结一下你的修改就最好了

不需要你进行测试啊，就是你只要静态的检测一下代码 还有就是用MCP检查控制台确认没有报错就行没问题就可以了，然后告诉我要怎么测试就可以了。
